\ProvidesPackage{resources/pjconfig}

\RequirePackage{ifthen}
\RequirePackage{geometry}
\RequirePackage{titlesec}
\RequirePackage{dirtree}
\RequirePackage{enumitem}
\RequirePackage{tocloft}
\RequirePackage{amssymb}

\newfontfamily\OCRAfont{ocr}[
  Path=./resources/fonts/,  
  Extension      = .ttf,
  UprightFont    = *-upright,
  LetterSpace=-7
  % Scale=1.1
]

% ====================================================================================================
% English version below.
% 
% Instrukcje edycji:
% 1. W komendach typu \pjfaculty czy \pjdepartment należy zastąpić całość ciała odpowiednim tekstem. Nie przejmujemy się formatowaniem - wpisujemy jedynie tekst.
% 
% Editing instructions:
% 1. In commands such as \pjfaculty or \pjdepartment you should replace the entirely of its body with sufficient text. Don't worry about formatting - input just text.
% ====================================================================================================

%%% ##### "configuration options start" ##### %%%
\newcommand{\forprinting}{false} % true or false

\newcommand{\pjlanguage}{english} % either english or polish - this affects some autogenerated elements

\newcommand{\pj}{
\ifthenelse
    {\equal{\pjlanguage}{english}}
    {\hspace*{-2pt}{\OCRAfont PJAIT}\hspace*{-2pt}}
    {\hspace*{-2pt}{\OCRAfont PJATK}\hspace*{-2pt}}
}

\newcommand{\pjfaculty}{
\ifthenelse
    {\equal{\pjlanguage}{english}}
    {Name of the faculty (e.g., Faculty of Information Technology)}
    {Nazwa wydziału (np. Wydział Informatyki)}
}

\newcommand{\pjdepartment}{
\ifthenelse
    {\equal{\pjlanguage}{english}}
    {Name of the department (e.g., Department of Software Engineering)}
    {Nazwa katedry (np. Inżynieria Oprogramowania)}
}

\newcommand{\pjspecialization}{
\ifthenelse
    {\equal{\pjlanguage}{english}}
    {Name of the specialization (e.g., Software and Database Engineering)}
    {Nazwa specjalizacji (np. Inżynieria Oprogramowania i Baz Danych)}
}

\newcommand{\pjauthors}{
\ifthenelse
    {\equal{\pjlanguage}{english}}
    {Your name and surname, s\#\#\#\#\#}
    {Twoje imię i nazwisko, s\#\#\#\#\#}
}

\newcommand{\pjtitle}{
\ifthenelse
    {\equal{\pjlanguage}{english}}
    {Your thesis title}
    {Tytuł Twojej pracy dyplomowej}
}

\newcommand{\pjsupervisor}{
\ifthenelse
    {\equal{\pjlanguage}{english}}
    {Name of your supervisor (and their academic title)}
    {Imię i nazwisko promotora (wraz z tytułem / stopniem)}
}

\newcommand{\pjauxsupervisor}{
\ifthenelse
    {\equal{\pjlanguage}{english}}
    {Name of your auxiliary supervisor (and their academic title)}
    {Imię i nazwisko promotora pomocniczego / recenzenta (wraz z tytułem / stopniem)}
}

\newcommand{\pjdefencedateandloc}{
\ifthenelse
    {\equal{\pjlanguage}{english}}
    {Warsaw, February 2030}
    {Warszawa, luty 2023}
}

\newcommand{\pjthesistypeandsupervisortext}{
    \ifthenelse
    {\equal{\pjlanguage}{english}}
    {\raggedright Master's degree / Bachelor's degree thesis written under the supervision of:} 
    {\raggedright Praca magisterska / Praca inżynierska napisana pod kierunkiem:} 
}

\setmonofont{JetBrainsMono}[
  Path=./resources/fonts/,
  Extension      = .ttf,
  UprightFont    = *-SemiBold,
  % UprightFont    = *-Regular,
  ItalicFont     = *-Italic,
  BoldFont       = *-ExtraBold,
  BoldItalicFont = *-ExtraBoldItalic,
  % BoldFont       = *-Bold,
  % BoldItalicFont = *-BoldItalic,
  Scale=1.1
]

\newfontfamily\JBMmdfamily{JetBrainsMono}[
  Path=./resources/fonts/,
  Extension      = .ttf,
  UprightFont    = *-SemiBold,
  % UprightFont    = *-Regular,
  ItalicFont     = *-Italic,
  BoldFont       = *-ExtraBold,
  BoldItalicFont = *-ExtraBoldItalic,
  % BoldFont       = *-Bold,
  % BoldItalicFont = *-BoldItalic,
  Scale=1.1
]

\definecolor{background}{rgb}{1.0, 1.0, 1.0}
\definecolor{foreground}{rgb}{0.1, 0.1, 0.1}
\definecolor{keyword}{rgb}{0.0, 0.0, 0.9}
\definecolor{comment}{rgb}{0.4, 0.4, 0.4}
\definecolor{string}{rgb}{0.0, 0.6, 0.0}
\definecolor{number}{rgb}{0.0, 0.4, 0.0}

\lstdefinestyle{intellijlight}{
    extendedchars=true,
    morekeywords={var},
    backgroundcolor=\color{background},
    basicstyle=\ttfamily\scriptsize,
    keywordstyle=\color{keyword},
    commentstyle=\color{comment}\itshape,
    stringstyle=\color{string},
    numbers=left,
    numberstyle=\transparent{0.4}\ttfamily,
    numbersep=10pt,
    showstringspaces=false,
    breaklines=true,
    frame=lines,
}

\lstdefinelanguage{Kotlin}{
  comment=[l]{//},
  commentstyle={\color{gray}\ttfamily},
  identifierstyle=\color{black},
  keywords={!in, !is, abstract, actual, annotation, as, as?, break, by, catch, class, companion, const, constructor, continue, crossinline, data, delegate, do, dynamic, else, enum, expect, external, false, field, file, final, finally, for, fun, get, if, import, in, infix, init, inline, inner, interface, internal, is, lateinit, noinline, null, object, open, operator, out, override, package, param, private, property, protected, public, receiveris, reified, return, return@, sealed, set, setparam, super, suspend, tailrec, this, throw, true, try, typealias, typeof, val, var, vararg, when, where, while},
  keywordstyle={\color{NavyBlue}\bfseries},
  morecomment=[s]{/*}{*/},
  morestring=[b]",
  morestring=[s]{"""*}{*"""},
  ndkeywords={@Deprecated, @JvmField, @JvmName, @JvmOverloads, @JvmStatic, @JvmSynthetic, Array, Byte, Double, Float, Int, Integer, Iterable, Long, Runnable, Short, String, Any, Unit, Nothing},
  ndkeywordstyle={\color{BurntOrange}\bfseries},
  sensitive=true,
  stringstyle={\color{ForestGreen}\ttfamily},
}

\lstdefinestyle{inline}{
    basicstyle=\ttfamily\scriptsize,
    keywordstyle=,
    breaklines=true,
    showstringspaces=false
}

\lstdefinestyle{plaintext}{
    keywordstyle=,     % No special style for keywords
    identifierstyle=,  % No special style for identifiers
    commentstyle=,     % No special style for comments
    stringstyle=,      % No special style for strings
}

\lstset{language=Java, numbers=left, showspaces=false,
    showstringspaces=false, tabsize=2, breaklines=true,
    xleftmargin=5.0ex,style=intellijlight,
    numberbychapter=false
}

\newcommand{\pjref}[1]{\textbf{\ref{#1} \nameref{#1}}}
%%% ##### "configuration options end" ##### %%%













%%% ##### "better not touch" start ##### %%%
\newcommand{\pjinsertlogo}{
    \ifthenelse
    {\equal{\pjlanguage}{english}}
    {\includegraphics[width=\linewidth]{resources/logo/PJAIT_EN_poziom_1}}
    {\includegraphics[width=\linewidth]{resources/logo/PJATK_PL_poziom_1}}
}

\ifthenelse
    {\equal{\forprinting}{true}}
    {
        \newgeometry{
            top=1in,
            bottom=1.25in,
            outer=1.25in,
            inner=2.25in
        }
    }
    {
        \newgeometry{
            top=1in,
            bottom=1.25in,
            outer=1.5in,
            inner=1.5in
        }
    }

\titleformat{\chapter}[hang] % 'hang' keeps the title on a new line without page break
    {\normalfont\LARGE\bfseries} % Font style: large, bold, normal font
    {\thechapter.}{1em} % Format chapter number with a dot and some space
    {}

\titlespacing*{\chapter}{0pt}{1.5em}{0.5em}

\newcommand\mydirtree[1]{{\scriptsize\dirtree{#1}\par}}

\newcommand{\pjuniname}{\textbf{
    \ifthenelse
    {\equal{\pjlanguage}{english}}
    {{Polish-Japanese Academy of Information Technology}}
    {Polsko-Japońska Akademia Technik Komputerowych}
}}

\newenvironment{leftbox}[1]
 {\itemize[
    nosep,
    leftmargin=0pt,
    rightmargin=\dimexpr\textwidth-#1\relax,
    itemindent=\parindent,
    listparindent=\parindent,
  ]\item[]\relax}
 {\enditemize}

\newenvironment{rightbox}[1]
 {\itemize[
    nosep,
    leftmargin=\dimexpr\textwidth-#1\relax,
    rightmargin=0pt,
    itemindent=\parindent,
    listparindent=\parindent,
  ]\item[]\relax}
 {\enditemize}

\titleformat{\subsection}
    {\bfseries\large}{\thesubsection}{1em}{}

\renewcommand{\cftchapfont}{\scshape\bfseries}
\renewcommand\cftsubsecfont{\itshape}

\renewcommand{\cftaftertoctitle}{\hfill}

\newcommand{\pjabstract}{
    \ifthenelse
    {\equal{\pjlanguage}{english}}
    {Abstract}
    {Streszczenie}
}

\newcommand{\pjkeywords}{
    \ifthenelse
    {\equal{\pjlanguage}{english}}
    {Keywords}
    {Słowa kluczowe}
}

\newcommand{\pjcacknowledgements}{
    \ifthenelse
    {\equal{\pjlanguage}{english}}
    {Acknowledgements}
    {Podziękowania}
}

\ifthenelse
{\equal{\pjlanguage}{polish}}
{\renewcommand\lstlistlistingname{Listingi}}
{}

%%% ##### "better not touch" end ##### %%%
