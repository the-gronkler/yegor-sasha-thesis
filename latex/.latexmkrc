# Allow interruption of running builds
$pvc_view_file_via_temporary = 0;

# Use lualatex
$pdf_mode = 4;

# Output directory (current directory)
$out_dir = '.';

# Clean up auxiliary files
$clean_ext = 'aux bbl blg fdb_latexmk fls log synctex.gz';
