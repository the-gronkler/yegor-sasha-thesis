npx lint-staged || (echo "⚠️ Lint-staged failed..." && powershell.exe -NoProfile -Command "Add-Type -AssemblyName PresentationFramework; [System.Windows.MessageBox]::Show('Lint-staged failed, but committing anyway.', 'Pre-commit Warning')") || true
